# Changelog — 2026-06

### 2026-06-23 — chore(box/cachyos): re-stamp to schema 2026.174.0300 + migrate user/mount/kernel-param checks to plugin:

The state-provision verbs user, mount, kernel-param left the closed `#Op` into dedicated builtin
plugin units (charly superproject — the shared-path state-provision batch, on the proven act-emit
enabler). `charly migrate` re-stamps this repo's `charly.yml` to the new schema HEAD and converts any
`user:`/`mount:`/`kernel-param:` check/run steps to `plugin: <verb>` + `plugin_input` (each verb's
own fields move into its `#<Verb>Input`). Gate: `charly box validate` exit 0 (git-workflow B7).

### 2026-06-23 — chore(box/cachyos): re-stamp to schema 2026.174.0100 + migrate unix_group checks to plugin:

The unix_group state-provision verb left the closed `#Op` into a dedicated builtin plugin unit
(charly superproject — the FIRST state-provision verb, dual-natured: a check-verb AND a run-verb→act).
`charly migrate` re-stamps this repo's `charly.yml` to the new schema HEAD and converts any
`unix_group:` check/run steps to `plugin: unix_group` + `plugin_input` (the shared `gid` companion,
read by the `user` verb, stays in `#Op`). Gate: `charly box validate` exit 0 (git-workflow B7).

### 2026-06-22 — fix(box/cachyos): commit the box/versa http→plugin migration omitted from the wave-2 re-stamp

The observe-verb wave-2 re-stamp (previous commit) migrated `box/versa/charly.yml`'s `http:` check
steps to `plugin: http` + `plugin_input` via `charly migrate`, but the landing staged only the root
`charly.yml` + CHANGELOG, so `box/versa` was left uncommitted (its `http:` authoring would fail under
the new schema `2026.173.2300`). This commits the omitted migration. Gate: `charly box validate` exit
0 (the versa box's plugin: http checks validate at the current schema).

The observe verbs http/interface/addr left the closed `#Op` into dedicated builtin plugin units
(charly superproject, observe-verb wave 2). `charly migrate` re-stamps this repo's `charly.yml` to
the new schema HEAD and converts any `http:`/`interface:`/`addr:` CHECK steps to `plugin: <verb>` +
`plugin_input:` (the shared `method`/`request_body`/`timeout` modifiers stay in `#Op` — they're
read by the live cdp/dbus/libvirt verbs too). Gate: `charly box validate` exit 0 (git-workflow B7).

### 2026-06-22 — chore(box/cachyos): re-stamp to schema 2026.173.1742 + migrate goss-verb checks to plugin:

The observe-only goss verbs (port/process/dns) left the closed `#Op` into dedicated builtin
plugin units (charly superproject). `charly migrate` re-stamps this repo's `charly.yml` to the
new schema HEAD and converts any `port:`/`process:`/`dns:` CHECK steps to `plugin: <verb>` +
`plugin_input:` (a box's published `port:` + install `package:`/`service:` are isStepNode-gated
and untouched). Gate: `charly box validate` exit 0 (git-workflow B7 schema-HEAD re-stamp).

### 2026-06-22 — chore(box/cachyos): re-stamp to schema 2026.173.1616 (matching-to-plugin cutover)

The `matching` check verb left the closed `#Op` schema (the charly superproject added a dedicated
matching plugin unit + the `matching-to-plugin` MigrationStep raising `LatestSchemaVersion`
2026.172.0006 → 2026.173.1616). `charly migrate` re-stamps this repo's `charly.yml` `version:` to
the new schema HEAD; no content change here (matching was already migrated / unused). Gate:
`charly box validate` exit 0 (git-workflow B7 schema-HEAD re-stamp).

### 2026-06-21 — chore(box/cachyos): migrate to schema 2026.172.0006 (EDGE-INHERIT cutover D — `box:` merged into `candy:`)

`charly migrate` applies the superproject's EDGE-INHERIT cutover D (the `box:` KIND merges INTO `candy:` —
`base:`/`from:` presence makes a candy a full IMAGE) to this submodule's per-box dirs: every discovered
`box/<name>/charly.yml` image renames its `box:` KIND discriminator to `candy:` (keeping its `base:`/`from:`
marker, which is what now makes it an image), and the schema HEAD is re-stamped to `2026.172.0006`. The
`box:` IMAGE cross-ref FIELD on the `android:` device entry (`pixel9a-36`) is a FIELD, not a discriminator, and
is left untouched. No behavior change — the image is identical, only the kind-at-edge spelling moves. (R10 ran
the check-cachyos-vm bed — PASS.)

### 2026-06-21 — chore(box/cachyos): migrate to schema 2026.172.0004 (EDGE-INHERIT cutover B — `bundle:` eliminated)

`charly migrate` applies the superproject's EDGE-INHERIT cutover B (the `bundle:` deploy KIND is eliminated —
substrate kinds at the EDGE) to this submodule's `charly.yml`: every disposable check bed / deploy moves from
`bundle:{box:X}` to `pod:{image:X}` (and `bundle:{vm:V}`→`vm:{from:V}`, a targetless `bundle:`→`group:`),
and the schema HEAD is re-stamped to `2026.172.0004`. No behavior change — the deploy shape is identical, only
the kind-at-edge spelling moves. (R10 for the cutover ran check-versa-pod / check-android-emulator-pod — PASS.)

### 2026-06-21 — chore(box/cachyos): migrate to schema 2026.172.0002 (EDGE-INHERIT cutover A probe-verb rename)

`charly migrate` re-stamps the submodule to the new schema HEAD `2026.172.0002` for the superproject's
EDGE-INHERIT cutover A (the step probe verbs `k8s`/`group` → `kube`/`unix_group`). This submodule declares
no colliding step verb, so the only change is the version re-stamp; no behavior change.

### 2026-06-19 — feat: adopt v2026.170.2016 candy pins (the kde-selkies plasmashell fix) (`v2026.170.2022`)

`charly box reconcile --remote` bumped every `@github.com/overthinkos/overthink/candy/*` pin (root
`charly.yml` + the per-box `charly.yml` across 21 files) from v2026.170.1105 → **v2026.170.2016**,
propagating the **kde-selkies plasmashell-launch fix** — the streamed KDE Plasma desktop now renders its
panel + wallpaper instead of a black void (superproject `v2026.170.2016`) — plus the intervening
comment-only doc sweeps (#14/#15/#16, no behavior change). Standalone `charly box validate` (no
`CHARLY_REPO_OVERRIDE`) passes against the real remote tag, confirming the reconciled pins resolve with no
transitive drift. The `arch` namespace import is unchanged (#13 never touched arch).

---

### 2026-06-19 — docs: sweep stale build.yml refs in charly.yml comments (`v2026.170.1736`)

4 comment refs in `charly.yml` ("the main repo's build.yml" / "build.yml distro.cachyos.pacstrap...") rewritten to the embedded build vocabulary — §3 dropped `build.yml`; the vocab is embedded at `charly/charly.yml`. Comment-only.

---

### 2026-06-19 — feat: adopt v2026.170.1105 candy pins + v2026.170.1539 arch import (`v2026.170.1539`)

`charly box reconcile --remote` bumped all 171 `@github.com/overthinkos/overthink/candy/*` pins from
v2026.166.1222 → v2026.170.1105, AND re-pinned the `arch:` namespace import → **v2026.170.1539** (the
fresh arch pin-bump tag). That second bump is load-bearing: the §3-landed arch tag (v2026.170.1105) still
carried stale @166 candy pins, so a standalone `charly box validate` transitively fetched
`overthink:v2026.166.1222` and failed on the `k8s` step-venue collision; pinning the re-bumped arch makes
the imported arch carry @170 pins. Standalone `charly box validate` (no `CHARLY_REPO_OVERRIDE`) now passes.

---

### 2026-06-19 — feat!: migrate to the unified name-first node model + R5 stale-ref sweep (`v2026.170.1105`)

Every `charly.yml` entity migrates to the unified `<name>: {<kind>: …, <children>}`
node form: `deploy:`→`bundle:`, venue + membership from TREE POSITION (authored
`on:`/`pod:`/`peer:`/`nested:`/`target:` removed), `${HOST:<member>}` the sole
cross-member address, `check:` beds → disposable bundles. R5 sweep removes every
reference to the deleted identifiers — including the functional `charly deploy
from-box --help` / `charly deploy --help` check commands → `charly bundle …`,
plus `${PEER_ENDPOINT}`/`${PEER_HOST}`→`${HOST:…}` and `DeploymentNode`→`BundleNode`
in prose. Validated via the local-main override; the standalone candy `@github`
pins bump in the immediate-next `charly box reconcile` cutover. Full change:
superproject `CHANGELOG/`.

---

### 2026-06-17 — chore(changelog): adopt the per-repo monthly `CHANGELOG/` structure

This repository now keeps its own `CHANGELOG/` directory with one file per
calendar month (`YYYY-MM.md`), matching the project-wide history model in which
the superproject and every submodule keeps its OWN `CHANGELOG/`. History is
repo-scoped, never centralized in a single root file. The repo's signpost
(`CLAUDE.md` / `README.md`) was updated to point here. See the superproject
`CLAUDE.md` "Where things are documented" and `/charly-internals:skills`.
